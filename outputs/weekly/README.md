# Weekly rollups (JSON-first)

Use `doc_type: "weekly_digest"` per `templates/schemas/weekly-digest.schema.json`. Run `./scripts/weekly-rollup.sh` for a scaffold + prompt.

Legacy weekly markdown is archived under `archive/legacy-outputs/weekly/` when migrated.
